.DS_Store
.serena/

# Secret local (contient les vraies clés API) — jamais commité.
# Le gabarit .env.example et la config .mcp.json (placeholders ${...}) sont trackés.
.env

# Secret d'infra Graphiti (clé X-API-Key du rappel épisodique, STD-038) — jamais commité.
graphiti*.env
**/graphiti*.env

# Artefact de build du CLI (payload bundlé, reconstruit au publish)
cli/payload/

# Canvas Excalidraw — runtime (les schémas eux-mêmes vivent dans docs/diagrams/)
.herdr/
